# Test Requirements - Lightweight version for CI/CD
# Excludes heavy ML dependencies (torch, transformers, whisper) that aren't needed for tests

# Video and Image Processing (needed for TTV tests)
opencv-python>=4.9.0.80
moviepy>=1.0.3

# Data Processing and Scientific Computing (needed for various modules)
numpy>=1.24.3
scipy>=1.10.1

# API Clients (needed for tests)
openai>=1.3.0
google-cloud-texttospeech>=2.14.1
google-cloud-storage>=2.10.0

