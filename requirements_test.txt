# Test Requirements - Includes dependencies needed for test collection
# Some modules import these at top-level, so they're required even for tests

# ML Dependencies (needed for audio_alignment.py and music_backends/meta.py imports)
torch>=2.0.0
torchaudio>=2.0.0
openai-whisper>=20231117
transformers>=4.30.0

# Video and Image Processing (needed for TTV tests)
opencv-python>=4.9.0.80
moviepy>=1.0.3

# Data Processing and Scientific Computing (needed for various modules)
numpy>=1.24.3
scipy>=1.10.1

# API Clients (needed for tests)
openai>=1.3.0
google-cloud-texttospeech>=2.14.1
google-cloud-storage>=2.10.0

